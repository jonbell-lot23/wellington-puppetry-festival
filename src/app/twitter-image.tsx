// X/Bluesky read twitter:image in preference to og:image. Same card, same
// size — re-exported rather than duplicated so the two can't drift.
export { default, alt, size, contentType } from './opengraph-image'
